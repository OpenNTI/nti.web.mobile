import React from 'react';
import Button from 'common/forms/components/Button';

export default React.createClass({
	displayName: 'CommunityJoinButton',

	render () {
		return (
			<Button className="follow-button">Join Community</Button>
		);
	}
});
